# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in fiber_pool.gemspec
gemspec

gem 'rake', '~> 13.0'

# Test Tools
gem 'guard-rspec', require: false
gem 'rspec', '~> 3.0'
gem 'rubocop', '~> 1.21'

# Non blocking IO and FiberSchedulers
gem 'async'

# Redis for testing the connection pools
gem 'redis'
gem 'async-redis'
